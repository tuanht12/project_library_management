/**
 *  @brief Hàm hỗ trợ ghi dữ liệu sách vào file CSV sau khi thoát chương trình.
 */
void write_books_to_csv();

/**
 *  @brief Hàm hỗ trợ đọc dữ liệu sách từ file CSV khi khởi động chương trình.
 */
int read_books_from_csv();

/**
 *  @brief Hàm hỗ trợ ghi dữ liệu người dùng vào file CSV sau khi thoát chương
 * trình.
 */
void write_users_to_csv();

/**
 *  @brief Hàm hỗ trợ đọc dữ liệu người dùng từ file CSV khi khởi động chương
 * trình.
 */
int read_users_from_csv();

/**
 *  @brief Hàm hỗ trợ ghi dữ liệu phiếu mượn sách vào file CSV sau khi thoát
 * chương trình.
 */
void write_borrows_to_csv();

/**
 *  @brief Hàm hỗ trợ đọc dữ liệu phiếu mượn sách từ file CSV khi khởi động
 * chương trình.
 */
int read_borrows_from_csv();
